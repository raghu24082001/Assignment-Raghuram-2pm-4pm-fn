import React from 'react'
  

const App = () => {
  //redux is state management library used with react to mannage global application state(data shared across components)

  //store

  // store is a centralized place were appication state stores.
  // only one store for react app.

  //slice
  //slice is a piece of redux state + logic .

  //state + reduce + actions
  //reduces boiler plate
  //feature based structure

  //reducer is fuction update state based on their action

  //no api calls
  //no side effect

  //action is event that tells something to react

  //dispatch is used to send action to react.
  
  //selector in redux
  //selector is used to readd state from the store.

  //component re-render only when selected state changes
  return (
    <div>App
      
    </div>
  )
}

export default App